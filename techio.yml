title : SASS
plan:
  - title: SASS Basics Duplicate Playground (Just for testing)
    statement: markdowns/welcome.md
projects:
  sass:
   root: root
   buildCommand: "cd /project/target && chmod +x prebuild.sh && ./prebuild.sh"
   runner:
    name: ruby
    version: latest
